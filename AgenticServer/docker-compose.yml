services:
  agent_server:
    build: .
    volumes:
      - .:/app
    ports:
      - "8007:8007"
    env_file:
      - .env
    environment:
      - DEBUG=True
      - DJANGO_SETTINGS_MODULE=config.settings
    restart: unless-stopped

# Optional: Add Redis or MongoDB if required by future agents
#  mongodb:
#    image: mongo:latest
#    ports:
#      - "27017:27017"
#    volumes:
#      - mongodb_data:/data/db

# volumes:
#   mongodb_data:
