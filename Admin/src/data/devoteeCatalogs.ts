// Mock catalog data for the devotee portal (seva, accommodation, prasadam, facilities).
// Kept separate from the i18n system since a future phase will replace these mocks
// with real API-driven catalogs — this file only owns the shape/content of the data.

export type CatalogLanguage = 'en' | 'te' | 'hi'
